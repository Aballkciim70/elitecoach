/* KPSUL TODAY V1 — tableau de bord intelligent */
(() => {
  'use strict';
  const $=id=>document.getElementById(id), sb=()=>window.sb||null;
  const esc=v=>String(v??'').replace(/[&<>"']/g,s=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[s]));
  const today=()=>new Date().toISOString().slice(0,10);
  const daysBack=n=>new Date(Date.now()-n*86400000).toISOString().slice(0,10);
  const avg=a=>a.length?a.reduce((x,y)=>x+y,0)/a.length:0;
  const clamp=(v,a=0,b=100)=>Math.max(a,Math.min(b,Number(v)||0));
  let uid=null, profile={}, loaded=false;

  function styles(){if($('ktStyle'))return;const s=document.createElement('style');s.id='ktStyle';s.textContent=`
  .kt-tile{border-color:rgba(52,224,200,.45)!important;background:linear-gradient(145deg,rgba(52,224,200,.1),rgba(255,255,255,.02))!important}
  .kt-hero,.kt-card{border:1px solid var(--line,#22403A);border-radius:18px;background:rgba(255,255,255,.025)}
  .kt-hero{padding:20px;margin-bottom:14px;background:radial-gradient(circle at top right,rgba(52,224,200,.14),transparent 45%),rgba(255,255,255,.025)}
  .kt-kicker{font:10px var(--mono,monospace);letter-spacing:.16em;text-transform:uppercase;color:var(--core,#34E0C8)}
  .kt-hero h3{font:700 clamp(26px,5vw,42px) var(--disp,system-ui);margin:7px 0 3px}.kt-date{color:#8A9A93;font-size:13px}
  .kt-scores{display:grid;grid-template-columns:repeat(3,1fr);gap:9px;margin-top:16px}.kt-score{padding:12px;text-align:center;border:1px solid var(--line,#22403A);border-radius:13px;background:rgba(3,10,8,.35)}
  .kt-score b{display:block;font:700 24px var(--disp,system-ui);color:var(--core,#34E0C8)}.kt-score span{font-size:11px;color:#8A9A93}
  .kt-grid{display:grid;grid-template-columns:1.15fr .85fr;gap:13px}.kt-card{padding:16px;min-width:0}.kt-card h4{margin:0 0 11px;font:700 18px var(--disp,system-ui)}
  .kt-priorities,.kt-list{display:grid;gap:8px}.kt-action{border:1px solid var(--line,#22403A);border-radius:14px;padding:13px;background:rgba(3,10,8,.34)}
  .kt-top{display:flex;justify-content:space-between;gap:10px}.kt-action b{font:700 15px var(--disp,system-ui)}.kt-action p{margin:5px 0 0;color:#8A9A93;font-size:12.5px;line-height:1.45}
  .kt-impact{font:700 10px var(--mono,monospace);color:var(--core,#34E0C8);border:1px solid rgba(52,224,200,.3);border-radius:99px;padding:5px 8px;white-space:nowrap}.kt-action button{margin-top:10px}
  .kt-row{display:flex;justify-content:space-between;gap:12px;border-bottom:1px solid rgba(34,64,58,.7);padding:9px 0}.kt-row:last-child{border-bottom:0}.kt-row span{color:#8A9A93;font-size:12.5px}.kt-row b{text-align:right;font-size:13px}
  .kt-good{color:#62d8a3}.kt-warn{color:#E9C46A}.kt-bad{color:#E8735B}.kt-advice{border-left:3px solid var(--core,#34E0C8);padding:13px 14px;background:rgba(52,224,200,.055);border-radius:0 14px 14px 0;line-height:1.55;font-size:14px}
  .kt-message blockquote{margin:0;padding:12px;border:1px solid var(--line,#22403A);border-radius:14px;background:rgba(3,10,8,.35);font-size:13.5px}.kt-meta,.kt-empty{color:#8A9A93;font-size:12px;line-height:1.5}.kt-footer{display:flex;gap:9px;flex-wrap:wrap;margin-top:13px}.kt-status{text-align:center;min-height:17px;margin:8px 0;color:var(--core,#34E0C8);font-size:12px}.kt-status.err{color:#E8735B}
  @media(max-width:800px){.kt-grid{grid-template-columns:1fr}}@media(max-width:560px){.kt-scores{grid-template-columns:1fr 1fr}.kt-score:first-child{grid-column:1/-1}}
  `;document.head.appendChild(s)}

  function ensure(){styles();let p=$('modToday');if(!p){p=document.createElement('div');p.id='modToday';p.className='module-panel';$('memberTools')?.prepend(p)}
    if(p&&!p.dataset.ready){p.dataset.ready='1';p.innerHTML=`<div class="tool-panel"><div class="kt-hero"><div class="kt-kicker">— Ton centre de pilotage</div><h3 id="ktGreeting">Bonjour 👋</h3><div class="kt-date" id="ktDate"></div><div class="kt-scores"><div class="kt-score"><b id="ktScore">—%</b><span>Kpsul Score</span></div><div class="kt-score"><b id="ktConf">—%</b><span>Fiabilité</span></div><div class="kt-score"><b id="ktDelta">—</b><span>Depuis hier</span></div></div></div><div class="kt-status" id="ktStatus"></div><div class="kt-grid"><section class="kt-card"><h4>🎯 Tes priorités aujourd'hui</h4><div class="kt-priorities" id="ktPriorities"></div></section><section class="kt-card"><h4>📅 Aujourd'hui</h4><div class="kt-list" id="ktPlan"></div></section><section class="kt-card"><h4>📈 Ce qui te fait progresser</h4><div class="kt-list" id="ktGood"></div></section><section class="kt-card"><h4>⚠️ Ce qui te freine</h4><div class="kt-list" id="ktBad"></div></section><section class="kt-card"><h4>🤖 Analyse Kpsul</h4><div class="kt-advice" id="ktAdvice">Analyse en cours…</div></section><section class="kt-card"><h4>💬 Dernier message du coach</h4><div id="ktMessage"></div></section></div><div class="kt-footer"><button class="btn btn-primary" id="ktRefresh">Actualiser mon plan</button><button class="btn btn-ghost" data-open="modKpsulIndex">Voir mon score</button><button class="btn btn-ghost" data-open="modMessages">Messagerie</button></div></div>`;
      $('ktRefresh')?.addEventListener('click',()=>refresh(true));p.querySelectorAll('[data-open]').forEach(b=>b.addEventListener('click',()=>openModule(b.dataset.open)))}tile()}

  function tile(){const box=document.querySelector('#member .member-tiles');if(!box||box.querySelector('[data-goto="modToday"]'))return;const d=document.createElement('div');d.className='mtile kt-tile';d.tabIndex=0;d.dataset.goto='modToday';d.innerHTML='<div class="mtile-icon">☀️</div><h3>Aujourd’hui</h3><p>Ton plan concret du jour, calculé depuis tes vraies données.</p><span class="mtile-go">Voir mes priorités →</span>';const go=()=>{openModule('modToday');refresh(false)};d.onclick=go;d.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();go()}};box.prepend(d)}
  function openModule(id){document.querySelectorAll('#member .module-panel').forEach(p=>p.classList.remove('active'));const t=$(id);if(!t)return;t.classList.add('active');document.body.classList.add('kpsul-panel-open');setTimeout(()=>$('member')?.scrollIntoView({behavior:'smooth'}),40)}
  async function safe(name,fn){try{const r=await fn();if(r?.error){console.warn('Today '+name,r.error.message);return[]}return r?.data||[]}catch(e){console.warn('Today '+name,e);return[]}}
  async function fetchData(){const c=sb();if(!c)throw Error("Supabase n'est pas disponible");const {data,error}=await c.auth.getSession();if(error)throw error;uid=data?.session?.user?.id;if(!uid)throw Error('Utilisateur non connecté');const d30=daysBack(30),d7=daysBack(7);const pr=await safe('profil',()=>c.from('profiles').select('*').eq('id',uid).limit(1));profile=pr[0]||{};const [w,n,h,m,g,g4,s,msg,p,b]=await Promise.all([
    safe('workouts',()=>c.from('workout_exercise_logs').select('*').eq('user_id',uid).gte('log_date',d30).order('log_date',{ascending:false})),safe('nutrition',()=>c.from('nutrition_logs').select('*').eq('user_id',uid).gte('log_date',d30).order('log_date',{ascending:false})),safe('habits',()=>c.from('habit_logs').select('*').eq('user_id',uid).gte('log_date',d30).order('log_date',{ascending:false})),safe('measures',()=>c.from('body_measurements').select('*').eq('user_id',uid).order('log_date',{ascending:false})),safe('goals',()=>c.from('client_goals').select('*').eq('user_id',uid).order('created_at',{ascending:false}).limit(2)),safe('goalsv4',()=>c.from('client_goals_v4').select('*').eq('user_id',uid).eq('status','active').limit(1)),safe('score',()=>c.from('kpsul_score_daily').select('*').eq('user_id',uid).order('score_date',{ascending:false}).limit(2)),safe('messages',()=>c.from('messages').select('*').eq('client_id',uid).order('created_at',{ascending:false}).limit(8)),safe('programs',()=>c.from('programs').select('*').eq('user_id',uid).order('created_at',{ascending:false}).limit(5)),safe('bookings',()=>c.from('bookings').select('*').eq('user_id',uid).gte('booking_date',today()).order('booking_date').limit(5))]);return{w,n,h,m,g,g4,s,msg,p,b,d7}}

  function analyse(D){const t=today(),w7=D.w.filter(x=>x.log_date>=D.d7),n7=D.n.filter(x=>x.log_date>=D.d7),h7=D.h.filter(x=>x.log_date>=D.d7);const wd=new Set(w7.map(x=>x.log_date)).size,nd=new Set(n7.map(x=>x.log_date)).size;const water=h7.map(x=>+x.water_liters).filter(x=>x>0),sleep=h7.map(x=>+x.sleep_hours).filter(x=>x>0),steps=h7.map(x=>+x.steps).filter(x=>x>0);const aw=avg(water),as=avg(sleep),ap=avg(steps);const tw=D.w.filter(x=>x.log_date===t),tn=D.n.filter(x=>x.log_date===t),th=D.h.find(x=>x.log_date===t);const score=Number(D.s[0]?.global_score),conf=Number(D.s[0]?.confidence_score),prev=Number(D.s[1]?.global_score);const priorities=[],good=[],bad=[],plan=[];
    if(!tw.length){priorities.push({i:'🏋️',t:'Vérifie ta séance du jour',x:wd?`${wd} jour(s) de séance sur 7.`:'Aucune séance enregistrée cette semaine.',impact:'fort impact',go:'modProgram',a:'Voir mon programme',sev:wd<2?95:65});plan.push({l:'Séance',v:'À vérifier',s:'warn'})}else{good.push({l:'Séance du jour',v:`${tw.length} exercice(s)`});plan.push({l:'Séance',v:'Commencée ou terminée',s:'good'})}
    if(!water.length||aw<2){const miss=Math.max(0,2.5-(+th?.water_liters||0));priorities.push({i:'💧',t:'Complète ton hydratation',x:water.length?`Moyenne ${aw.toFixed(1)} L/j. Environ ${miss.toFixed(1)} L manquant aujourd’hui.`:'Hydratation non renseignée récemment.',impact:'impact direct',go:'modHabits',a:'Renseigner mon eau',sev:80});bad.push({l:'Hydratation',v:water.length?`${aw.toFixed(1)} L/j`:'Non mesurée'})}else good.push({l:'Hydratation',v:`${aw.toFixed(1)} L/j`});
    if(!sleep.length||as<7){priorities.push({i:'😴',t:'Protège ton sommeil',x:sleep.length?`Moyenne ${as.toFixed(1)} h. Cible : 7 h 30.`:'Sommeil non renseigné récemment.',impact:'récupération',go:'modHabits',a:'Renseigner mon sommeil',sev:75});bad.push({l:'Sommeil',v:sleep.length?`${as.toFixed(1)} h/nuit`:'Non mesuré'})}else good.push({l:'Sommeil',v:`${as.toFixed(1)} h/nuit`});
    if(nd<5){priorities.push({i:'🍽️',t:'Rends ton suivi nutrition plus régulier',x:`Repas renseignés sur ${nd}/7 jours.`,impact:'fiabilité',go:'modNutrition',a:'Ajouter un repas',sev:(7-nd)*11});bad.push({l:'Nutrition',v:`${nd}/7 jours`})}else good.push({l:'Nutrition',v:`${nd}/7 jours`});
    const lw=D.m[0],pw=D.m[1],age=lw?.log_date?Math.floor((Date.now()-new Date(lw.log_date))/86400000):null;if(!lw||age>7){priorities.push({i:'⚖️',t:'Mets à jour ton poids',x:lw?`Dernière mesure il y a ${age} jours.`:'Aucune mesure enregistrée.',impact:'suivi objectif',go:'modProgress',a:'Ajouter une mesure',sev:70});bad.push({l:'Poids',v:lw?`Il y a ${age} j`:'Absent'})}else if(pw){const d=+lw.weight_kg-(+pw.weight_kg);good.push({l:'Poids suivi',v:`${lw.weight_kg} kg (${d>=0?'+':''}${d.toFixed(1)} kg)`})}else good.push({l:'Poids suivi',v:`${lw.weight_kg} kg`});if(ap) (ap>=8000?good:bad).push({l:'Activité',v:`${Math.round(ap)} pas/j`});
    plan.push({l:'Repas renseignés',v:tn.length?`${tn.length} aujourd’hui`:'Aucun aujourd’hui',s:tn.length?'good':'warn'},{l:'Hydratation',v:th?.water_liters?`${th.water_liters} L`:'À renseigner',s:th?.water_liters?'good':'warn'},{l:'Sommeil',v:th?.sleep_hours?`${th.sleep_hours} h`:'À renseigner',s:th?.sleep_hours?'good':'warn'});if(D.b[0])plan.unshift({l:'Prochain coaching',v:`${D.b[0].booking_date||''} ${D.b[0].booking_time||D.b[0].start_time||''}`.trim(),s:'good'});
    priorities.sort((a,b)=>b.sev-a.sev);const top=priorities.slice(0,3),first=top[0];let advice=first?`${first.t} est actuellement l’action la plus utile. ${first.x} `:'Tes principaux indicateurs sont bien renseignés. Continue ton plan sans ajouter de travail inutile. ';if(wd>=3&&as&&as<7)advice+='Tu t’entraînes déjà assez : la récupération aura probablement plus d’effet qu’une séance supplémentaire.';else if(nd<4)advice+='Quelques jours de suivi nutritionnel supplémentaires rendront les recommandations plus précises.';else advice+='Traite cette priorité avant de vouloir tout changer en même temps.';const coach=D.msg.find(m=>m.sender==='coach'||m.sender_role==='coach'||m.sender_role==='admin');return{score:Number.isFinite(score)?Math.round(score):null,conf:Number.isFinite(conf)?Math.round(conf):null,delta:Number.isFinite(score)&&Number.isFinite(prev)?Math.round(score-prev):null,top,good:good.slice(0,5),bad:bad.slice(0,5),plan,advice,coach}}

  function rows(id,a,empty){const b=$(id);if(!b)return;b.innerHTML=a.length?a.map(r=>`<div class="kt-row"><span>${esc(r.l)}</span><b class="${r.s==='good'?'kt-good':r.s==='warn'?'kt-warn':''}">${esc(r.v)}</b></div>`).join(''):`<div class="kt-empty">${esc(empty)}</div>`}
  function render(A){const name=profile?.full_name?.trim()?.split(/\s+/)[0]||'';$('ktGreeting').textContent=`Bonjour${name?' '+name:''} 👋`;$('ktDate').textContent=new Date().toLocaleDateString('fr-FR',{weekday:'long',day:'numeric',month:'long',year:'numeric'});$('ktScore').textContent=A.score==null?'—%':A.score+'%';$('ktConf').textContent=A.conf==null?'—%':A.conf+'%';$('ktDelta').textContent=A.delta==null?'—':(A.delta>=0?'+':'')+A.delta;const p=$('ktPriorities');p.innerHTML=A.top.length?A.top.map((x,i)=>`<article class="kt-action"><div class="kt-top"><div><b>${i+1}. ${x.i} ${esc(x.t)}</b><p>${esc(x.x)}</p></div><span class="kt-impact">${esc(x.impact)}</span></div><button class="btn btn-ghost" data-target="${x.go}">${esc(x.a)}</button></article>`).join(''):'<div class="kt-empty">Aucune urgence détectée.</div>';p.querySelectorAll('[data-target]').forEach(b=>b.onclick=()=>openModule(b.dataset.target));rows('ktPlan',A.plan,'Aucune donnée aujourd’hui.');rows('ktGood',A.good,'Renseigne quelques jours de données.');rows('ktBad',A.bad,'Aucun frein majeur détecté.');$('ktAdvice').textContent=A.advice;const m=$('ktMessage');if(A.coach){m.innerHTML=`<blockquote>${esc(A.coach.body||A.coach.message||'')}</blockquote><div class="kt-meta">${esc(new Date(A.coach.created_at).toLocaleString('fr-FR'))}</div><button class="btn btn-ghost" id="ktReply">Répondre</button>`;$('ktReply').onclick=()=>openModule('modMessages')}else m.innerHTML='<div class="kt-empty">Aucun message récent du coach.</div>'}
  function status(t,e=false){const x=$('ktStatus');if(x){x.textContent=t||'';x.classList.toggle('err',e)}}async function refresh(manual=false){ensure();try{if(manual)status('Analyse en cours…');const D=await fetchData();render(analyse(D));loaded=true;status(manual?'✔ Plan actualisé depuis tes vraies données.':'')}catch(e){console.error('Kpsul Today',e);status('Impossible de charger le plan : '+(e.message||e),true)}}
  function start(){ensure();window.KpsulToday={open:()=>{openModule('modToday');refresh(false)},refresh:()=>refresh(true)};if(document.body.classList.contains('authed'))refresh(false);new MutationObserver(()=>{if(document.body.classList.contains('authed')&&!loaded)refresh(false)}).observe(document.body,{attributes:true,attributeFilter:['class']})}
  document.readyState==='loading'?document.addEventListener('DOMContentLoaded',start):start();
})();
